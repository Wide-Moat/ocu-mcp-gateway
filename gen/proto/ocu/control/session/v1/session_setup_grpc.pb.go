// SPDX-License-Identifier: FSL-1.1-Apache-2.0
// Copyright (c) 2025 Open Computer Use Contributors
//
// Session-setup gRPC surface (MCP gateway -> Control plane). SERVICE-identity
// inbound: the caller is an in-workforce service authenticated by its verified
// peer identity (mTLS SAN), NOT an operator. Unary Create / Route / Destroy,
// the v1 lifecycle the gateway drives over the service ingress. NFR-IC-04
// (additive-only interface evolution), NFR-SEC-43 (host-derived binding, body
// id is a hint), NFR-SEC-26 / NFR-SEC-52 (this audience cannot reach
// force-kill / denylist-edit / quota-override), NFR-SEC-38 (runtime tier is
// deployment-wide, never a per-request field).
//
// STATUS: draft (#205, define-surface). This file formalizes the wire shape of
// the in-process gateway service surface (internal/ingress/gateway). The freeze
// lands in canon contracts/proto/ via the gatekeeper canon-PR; until then the
// CreateRequest / CreateResponse shapes below are the proposed wire, and the one
// known reconciliation point is carried as an open question (see the comment on
// CreateRequest.image_pin: the in-process gateway CreateRequest carries an
// `image` ref that is ABSENT from the frozen proto CreateRequest, PIN-PENDING).
//
// Additive-only (buf breaking gate, 08-contracts §4): field numbers are stable;
// a new field takes the next free number, an enum value is only appended, and a
// removed field is reserved rather than renumbered. No field is renumbered here.
//
// NFR-SEC-26 SURFACE INVARIANT: the service audience reachable through this
// proto CANNOT reach force-kill, denylist-edit, or quota-override. Those verbs
// are ABSENT from this service by design — the absence is the invariant, not an
// oversight. They live only on the operator ingress (contracts/openapi/
// operator-rest.openapi.yaml), which no gateway route reaches. Adding a
// privileged operator verb to this service would breach NFR-SEC-26; it must
// never appear here.
//
// CUSTODY: no request or response body on this surface ever carries the minted
// Storage-JWT, the real filestore credential, or any backend secret. Control
// mints the weak Storage-JWT and pushes it on the separate mount-config plane
// (contracts/storage/mount-config.schema.json); a create carrying it on the
// wire would be a custody leak. mount_intent therefore OMITS the auth token.

// Code generated by protoc-gen-go-grpc. DO NOT EDIT.
// versions:
// - protoc-gen-go-grpc v1.6.2
// - protoc             (unknown)
// source: ocu/control/session/v1/session_setup.proto

package sessionv1

import (
	context "context"
	grpc "google.golang.org/grpc"
	codes "google.golang.org/grpc/codes"
	status "google.golang.org/grpc/status"
)

// This is a compile-time assertion to ensure that this generated file
// is compatible with the grpc package it is being compiled against.
// Requires gRPC-Go v1.64.0 or later.
const _ = grpc.SupportPackageIsVersion9

const (
	SessionSetup_Create_FullMethodName  = "/ocu.control.session.v1.SessionSetup/Create"
	SessionSetup_Route_FullMethodName   = "/ocu.control.session.v1.SessionSetup/Route"
	SessionSetup_Destroy_FullMethodName = "/ocu.control.session.v1.SessionSetup/Destroy"
)

// SessionSetupClient is the client API for SessionSetup service.
//
// For semantics around ctx use and closing/ending streaming RPCs, please refer to https://pkg.go.dev/google.golang.org/grpc/?tab=doc#ClientConn.NewStream.
//
// SessionSetup is the gateway service-identity lifecycle surface. The caller's
// authority is its host-attested service identity (the verified mTLS peer SAN),
// established at the transport before any RPC body is read; every id-bearing
// field in a request body is a HINT the host may seed a derived binding from,
// never the authority (NFR-SEC-43). The service exposes ONLY Create / Route /
// Destroy: it is deliberately incapable of force-kill, denylist-edit, and
// quota-override (NFR-SEC-26 surface invariant).
type SessionSetupClient interface {
	// Create admits a session and materializes the sandbox after the admission +
	// quota gate runs fail-closed. The returned session_id is the host-derived
	// binding; a session/tenant/container_name hint in the request never becomes
	// that binding (NFR-SEC-43).
	Create(ctx context.Context, in *CreateRequest, opts ...grpc.CallOption) (*CreateResponse, error)
	// Route resolves a per-session control endpoint for an already-admitted
	// session the caller owns. Ownership is keyed on the host-attested caller
	// identity, not the body hint; a foreign session id is indistinguishable from
	// not-found (NFR-SEC-43).
	Route(ctx context.Context, in *RouteRequest, opts ...grpc.CallOption) (*RouteResponse, error)
	// Destroy tears the caller's OWN session down through the host-driven
	// finalizer. It is the cooperative service-side teardown, NOT the operator
	// force-kill — the privileged force path is absent from this service
	// (NFR-SEC-26). A foreign or absent session yields not-found, never another
	// tenant's teardown (NFR-SEC-43).
	Destroy(ctx context.Context, in *DestroyRequest, opts ...grpc.CallOption) (*DestroyResponse, error)
}

type sessionSetupClient struct {
	cc grpc.ClientConnInterface
}

func NewSessionSetupClient(cc grpc.ClientConnInterface) SessionSetupClient {
	return &sessionSetupClient{cc}
}

func (c *sessionSetupClient) Create(ctx context.Context, in *CreateRequest, opts ...grpc.CallOption) (*CreateResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(CreateResponse)
	err := c.cc.Invoke(ctx, SessionSetup_Create_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *sessionSetupClient) Route(ctx context.Context, in *RouteRequest, opts ...grpc.CallOption) (*RouteResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(RouteResponse)
	err := c.cc.Invoke(ctx, SessionSetup_Route_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *sessionSetupClient) Destroy(ctx context.Context, in *DestroyRequest, opts ...grpc.CallOption) (*DestroyResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(DestroyResponse)
	err := c.cc.Invoke(ctx, SessionSetup_Destroy_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

// SessionSetupServer is the server API for SessionSetup service.
// All implementations must embed UnimplementedSessionSetupServer
// for forward compatibility.
//
// SessionSetup is the gateway service-identity lifecycle surface. The caller's
// authority is its host-attested service identity (the verified mTLS peer SAN),
// established at the transport before any RPC body is read; every id-bearing
// field in a request body is a HINT the host may seed a derived binding from,
// never the authority (NFR-SEC-43). The service exposes ONLY Create / Route /
// Destroy: it is deliberately incapable of force-kill, denylist-edit, and
// quota-override (NFR-SEC-26 surface invariant).
type SessionSetupServer interface {
	// Create admits a session and materializes the sandbox after the admission +
	// quota gate runs fail-closed. The returned session_id is the host-derived
	// binding; a session/tenant/container_name hint in the request never becomes
	// that binding (NFR-SEC-43).
	Create(context.Context, *CreateRequest) (*CreateResponse, error)
	// Route resolves a per-session control endpoint for an already-admitted
	// session the caller owns. Ownership is keyed on the host-attested caller
	// identity, not the body hint; a foreign session id is indistinguishable from
	// not-found (NFR-SEC-43).
	Route(context.Context, *RouteRequest) (*RouteResponse, error)
	// Destroy tears the caller's OWN session down through the host-driven
	// finalizer. It is the cooperative service-side teardown, NOT the operator
	// force-kill — the privileged force path is absent from this service
	// (NFR-SEC-26). A foreign or absent session yields not-found, never another
	// tenant's teardown (NFR-SEC-43).
	Destroy(context.Context, *DestroyRequest) (*DestroyResponse, error)
	mustEmbedUnimplementedSessionSetupServer()
}

// UnimplementedSessionSetupServer must be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedSessionSetupServer struct{}

func (UnimplementedSessionSetupServer) Create(context.Context, *CreateRequest) (*CreateResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method Create not implemented")
}
func (UnimplementedSessionSetupServer) Route(context.Context, *RouteRequest) (*RouteResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method Route not implemented")
}
func (UnimplementedSessionSetupServer) Destroy(context.Context, *DestroyRequest) (*DestroyResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method Destroy not implemented")
}
func (UnimplementedSessionSetupServer) mustEmbedUnimplementedSessionSetupServer() {}
func (UnimplementedSessionSetupServer) testEmbeddedByValue()                      {}

// UnsafeSessionSetupServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to SessionSetupServer will
// result in compilation errors.
type UnsafeSessionSetupServer interface {
	mustEmbedUnimplementedSessionSetupServer()
}

func RegisterSessionSetupServer(s grpc.ServiceRegistrar, srv SessionSetupServer) {
	// If the following call panics, it indicates UnimplementedSessionSetupServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	s.RegisterService(&SessionSetup_ServiceDesc, srv)
}

func _SessionSetup_Create_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(CreateRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(SessionSetupServer).Create(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: SessionSetup_Create_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(SessionSetupServer).Create(ctx, req.(*CreateRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _SessionSetup_Route_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(RouteRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(SessionSetupServer).Route(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: SessionSetup_Route_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(SessionSetupServer).Route(ctx, req.(*RouteRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _SessionSetup_Destroy_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(DestroyRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(SessionSetupServer).Destroy(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: SessionSetup_Destroy_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(SessionSetupServer).Destroy(ctx, req.(*DestroyRequest))
	}
	return interceptor(ctx, in, info, handler)
}

// SessionSetup_ServiceDesc is the grpc.ServiceDesc for SessionSetup service.
// It's only intended for direct use with grpc.RegisterService,
// and not to be introspected or modified (even as a copy)
var SessionSetup_ServiceDesc = grpc.ServiceDesc{
	ServiceName: "ocu.control.session.v1.SessionSetup",
	HandlerType: (*SessionSetupServer)(nil),
	Methods: []grpc.MethodDesc{
		{
			MethodName: "Create",
			Handler:    _SessionSetup_Create_Handler,
		},
		{
			MethodName: "Route",
			Handler:    _SessionSetup_Route_Handler,
		},
		{
			MethodName: "Destroy",
			Handler:    _SessionSetup_Destroy_Handler,
		},
	},
	Streams:  []grpc.StreamDesc{},
	Metadata: "ocu/control/session/v1/session_setup.proto",
}
